<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container-fluid">
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<%= link_to "AssessmentPortal", root_path, class: "nav-link text-light fontstyle" %>
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				<% if user_signed_in? %>
					<% if current_user.admin? %>
						<div class="d-flex align-items-end mx-4">
							<li class="nav-item">
								<%= link_to "Projects", admin_projects_path, class: "nav-link adminprojectpath" %>
							</li>
						</div>
					<% end %>
				<% end %>
				<% if user_signed_in? %>
					<div class="d-flex align-items-end">
      			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
							<li class="mx-4 nav-item"><%= link_to "Logout", destroy_user_session_path, method: 'delete', class: "btn btn-warning" %></li>
						</ul>
					</div>
				<% end %>
				<% if !user_signed_in? %>
					<div class="d-flex align-items-end mx-4">
      			<div class="navbar-nav ms-auto">
							<li class="nav-item">
								<%= link_to "New to Portal", new_user_registration_path, class: "btn btn-warning" %>
							</li>
							<li class="nav-item">
								<%= link_to "Log In", new_user_session_path, class: "mx-2 btn btn-warning ml-3" %>
							</li>
						</div>
					</div>
				<% end %>
			</ul>
		</div>
	</div>
</nav>